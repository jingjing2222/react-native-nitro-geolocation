const path = require('path');

module.exports = {
  dependencies: {
    'react-native-nitro-geolocation': {
      root: path.join(__dirname, '../../packages/react-native-nitro-geolocation'),
    },
  },
};
