---
"react-native-nitro-geolocation": minor
---

Extend the Modern API location error contract with `INTERNAL_ERROR`, `PLAY_SERVICE_NOT_AVAILABLE`, and `SETTINGS_NOT_SATISFIED`, and normalize native Modern API failures into structured `LocationError` objects while preserving the `/compat` legacy error shape.
