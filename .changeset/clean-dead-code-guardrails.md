---
"react-native-nitro-geolocation": patch
---

Add repository dead-code guardrails, clean stale package metadata, and remove confirmed unused example/native code. The web implementation now keeps browser helpers, watch state, and the React hook in separate modules while preserving the public API surface.
